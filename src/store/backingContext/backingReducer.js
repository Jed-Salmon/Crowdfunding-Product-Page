import defaultBackingState from "./BackingProvider";

const backingReducer = (state, action) => {
  if (action.type === "INCREASE_AMOUNT_BACKED") {
  }
  if (action.type === "INCREASE_TOTAL_BACKERS") {
  }
  if (action.type === "REDUCE_REWARD_QUANTITY") {
  }
};

export default backingReducer;
